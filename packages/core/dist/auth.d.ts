import type { BloomAuthConfig, BloomAuth } from './types';
export declare function bloomAuth(config?: BloomAuthConfig): BloomAuth;
export { User, Session, BloomAuthConfig, BloomAuth } from './types';
